# ML Car Price Prediction

Полнофункциональный проект машинного обучения для прогнозирования цены автомобилей на основе его характеристик.

## 📊 Описание проекта

Используется датасет `CarPrice_Assignment.csv` (205 автомобилей, 26 признаков) для обучения модели **RandomForestRegressor** с целью предсказания цены.

### Метрики модели:
- **RMSE**: 1821.47
- **R²**: 0.958 (отличное качество)

## 📁 Структура проекта

```
project/
├── car_price_prediction.ipynb      # Основной notebook с полным pipeline
├── CarPrice_Assignment.csv          # Исходный датасет
├── requirements.txt                 # Зависимости Python
├── models/
│   ├── rf_car_price.joblib         # Обученная модель
│   └── val_predictions.csv         # Предсказания на валидации
└── figures/
    ├── correlation_matrix.png
    ├── mean_price_top_brands.png
    ├── boxplot_price_top_brands.png
    ├── violin_price_by_carbody.png
    ├── violin_price_by_drivewheel.png
    └── feature_importances.png
```

## 🎯 Ключевые признаки (по важности)

1. **Engine Size** (51%) — объём двигателя
2. **Curb Weight** (32%) — вес автомобиля
3. **Highway MPG** (6%) — расход на трассе
4. **Horsepower** (4%) — мощность
5. **Car Width** (2%) — ширина кузова

## 💡 Основные выводы

- **Sedan** — самый дешёвый тип кузова ($14.3k в среднем)
- **Hardtop** — дорогой вариант ($22.2k)
- **RWD** привод дороже FWD в 2 раза ($19.9k vs $9.2k)
- Топ дорогие бренды: Porsche ($31.5k), BMW ($26.9k), Volvo ($17.5k)

## 🚀 Использование

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск notebook
```bash
jupyter notebook car_price_prediction.ipynb
```

### 3. Использование обученной модели
```python
import joblib
import pandas as pd

# Загрузить модель
model = joblib.load('./models/rf_car_price.joblib')

# Предсказать цену для новых данных
# X_new должен содержать все признаки, использованные при обучении
predictions = model.predict(X_new)
```

## 📚 Технологии

- **scikit-learn** — ML фреймворк
- **pandas, numpy** — обработка данных
- **matplotlib, seaborn** — визуализация
- **joblib** — сохранение моделей

## 📈 Pipeline обработки данных

1. **Загрузка и EDA** — анализ структуры данных, распределения
2. **Предобработка** — извлечение признаков, заполнение пропусков
3. **Нормализация** — StandardScaler для числовых, OneHotEncoder для категориальных
4. **Обучение** — RandomForest с 100 деревьями (80/20 split)
5. **Оценка** — RMSE, R², feature importances
6. **Визуализация** — 6 графиков с статистикой

## 📝 Примечания

- Модель достаточно хороша для production (R² = 0.958)
- Возможны улучшения: cross-validation, hyperparameter tuning, feature engineering
- Все графики сохранены в папку `figures/` в формате PNG (150 DPI)

---

**Автор**: User  
**Дата**: 30 января 2026  
**Язык**: Python 3.13
